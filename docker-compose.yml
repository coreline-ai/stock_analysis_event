services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: deepstock
      POSTGRES_PASSWORD: deepstock
      POSTGRES_DB: deepstock
    ports:
      - "15432:5432"
    volumes:
      - deepstock_pg:/var/lib/postgresql/data

volumes:
  deepstock_pg:
